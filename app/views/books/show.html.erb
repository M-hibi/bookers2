<%= render 'users/side', user: @user %>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
          <%= image_tag @user.get_profile_image(100,100) %>
          <%= link_to user_path, class: 'nav-link' %>
          <%= @book.title %>
          <%= @book.body %>
          <%= link_to"edit", edit_book_path, class: 'btn btn-success' %>
          <%= link_to "destroy", book_path, class: 'btn btn-danger', method: :delete %>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>
</main>

